<%= @email.state %>

<%- locals = {:f => f} %>

<%= hook :admin_email_form_fields, locals do %>
  
  <%= render "shared/error_messages", :target => @email %>
    
  <%= render "admin/emails/steps/#{@email.state}", :form => f %>
    
<% end %>
